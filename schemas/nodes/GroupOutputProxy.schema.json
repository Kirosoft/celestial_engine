{
  "$id": "GroupOutputProxy",
  "title": "GroupOutputProxy",
  "type": "object",
  "description": "Minimal schema for group output proxy nodes (virtual exit port).",
  "properties": {
    "id": { "type": "string" },
    "type": { "const": "GroupOutputProxy" },
    "name": { "type": "string" },
    "props": {
      "type": "object",
      "properties": {
        "port": { "type": "string" },
        "groupId": { "type": "string" },
        "kind": { "const": "output" }
      },
      "required": ["port", "groupId", "kind"],
      "additionalProperties": true
    },
    "position": {
      "type": "object",
      "properties": { "x": { "type": "number" }, "y": { "type": "number" } },
      "required": ["x", "y"],
      "additionalProperties": false
    }
  },
  "required": ["id", "type", "name", "props"],
  "additionalProperties": true
}
