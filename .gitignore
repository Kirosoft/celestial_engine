# Root level node_modules (if added later)
node_modules

# Next.js build output
apps/web/.next
apps/web/out

# TypeScript build info
*.tsbuildinfo

# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*

# Test coverage
coverage

# Playwright artifacts
playwright-report
playwright/.cache
trace.zip
test-results
*.trace.zip
*.playwright-debug

# Playwright blob storage (if enabled)
blob-report

# Vitest cache
.vitest
**/.vitest-cache

# Jest (if ever introduced)
jest-cache/
jest-html-reporters-*/
jest-results.json

# Temporary screenshots / videos from tests
**/screenshots/
**/videos/
**/artifacts/

# E2E ephemeral repo root & test fixtures
apps/web/.e2e-root
apps/web/.api-test-nodes
apps/web/.api-test-edges
apps/web/.test-nodes
apps/web/.schema-test-all
apps/web/.test-validate
apps/web/.api-test-groups
apps/web/.api-test-groups-subgraph
apps/web/.api-test-groups-ports
apps/web/.api-test-groups-internal
apps/web/.defaults-test-root
apps/web/test-results
apps/web/playwright-report
apps/web/blob-report
apps/web/tmp

# Local debug scratch files
*.local.json
*.sandbox.*
scratch/**
tmp/**
*.iml

# Local environment overrides
.env.local
.env.development.local
.env.test.local
.env.production.local

# Editor / OS
.DS_Store
Thumbs.db
*.swp
*.swo
/.idea
/.vscode/*
!.vscode/extensions.json

# Output / compiled artifacts (future packaging)
dist
build

# Generated index/events (keep actual repo state, not committed unless desired) - comment out if you want them versioned
apps/web/.e2e-root/.awb

# Lockfiles (uncomment to ignore if using a different package manager standardization)
# package-lock.json
# yarn.lock
# pnpm-lock.yaml
/apps/web/node_modules
