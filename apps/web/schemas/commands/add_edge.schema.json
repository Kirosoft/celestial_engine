{
  "$id": "awb://commands/add_edge",
  "$schema": "http://json-schema.org/draft-07/schema#",
  "title": "add_edge",
  "type": "object",
  "required": ["type", "sourceId", "targetId"],
  "properties": {
    "type": { "const": "add_edge" },
    "sourceId": { "type": "string" },
    "targetId": { "type": "string" },
    "kind": { "type": "string", "enum": ["flow", "data"] }
  },
  "additionalProperties": false
}
